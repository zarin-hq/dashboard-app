# Vercel Blob token for cloud screenshot storage (optional)
# Without this, the app falls back to IndexedDB for images and localStorage for state.
# Get a token from: Vercel Dashboard → Storage → Blob → Create Store
BLOB_READ_WRITE_TOKEN=
